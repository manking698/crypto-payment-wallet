import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Crypto Wallet S - Payment test"
};

export default function PaymentTestLayout({ children }: { children: React.ReactNode }) {
    return children;
}

